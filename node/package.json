{
  "name": "node",
  "version": "1.0.0",
  "description": "install node: ``` brew install node ``` generate a package.json: ``` npm init -y ```",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.1"
  }
}
